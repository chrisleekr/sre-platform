[project]
name = "sre-platform-docs"
version = "0.0.0"
description = "Documentation build environment for SRE Platform. Not published."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
docs = [
    "mkdocs>=1.6.1,<2",
    "mkdocs-material>=9.7.7,<10",
    "pymdown-extensions>=12.0.1,<13",
]

[tool.uv]
package = false
