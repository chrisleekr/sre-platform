{
  "name": "@sre/surface-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "NODE_ENV=development DASHBOARD_BASE_URL=${DASHBOARD_BASE_URL:-http://localhost:45173} bun run --watch --env-file=../../.env src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@sre/db": "workspace:*",
    "@sre/hub": "workspace:*",
    "@sre/surfaces": "workspace:*",
    "ioredis": "^6.0.0"
  }
}
